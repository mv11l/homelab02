machine:
  certSANs:
  install:
    disk: /dev/sdb
    wipe: true
  network:
    interfaces:
      - interface: eth1
        dhcp: true
cluster:
  allowSchedulingOnControlPlanes: true
  apiServer:
    extraArgs:
      advertise-address: "0.0.0.0"
    certSANs:
      - 127.0.0.1
